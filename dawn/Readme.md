##### Dawn - Minimal sysinfo

> Like neofetch, but worse :)

Install:

```
curl -fsSL https://raw.githubusercontent.com/normalbeans/pocket-dimension/master/dawn/dawn.sh -o /tmp/dawn && chmod +x /tmp/dawn && sudo mv /tmp/dawn /usr/local/bin/dawn
```

Execute:

```
dawn
echo "dawn" >> ~/.bashrc
```
